on:
  push:
    branches: [main, master]
    paths: ["README.qmd"]
  pull_request:
    paths: ["README.qmd"]

name: render-readme

permissions:
  contents: write

jobs:
  render-readme:
    uses: ggsegverse/.github/.github/workflows/render-readme.yaml@main
    with:
      readme-file: README.qmd
